# from django.urls import path
# from .views import camera_feed

# app_name = 'blink_model'

# urlpatterns =[
#     path('webcam/', camera_feed.as_view(), name = "open_webcam"),
#     #path('stats/', stats.as_view(), name = 'model_stats')
# ]
